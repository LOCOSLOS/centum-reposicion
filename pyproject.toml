[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "centum-forecasting"
version = "0.1.0"
description = "Motor base de pronóstico de demanda para Centum Reposición"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["forecasting"]
